{% extends "default.twig" %}
{% block content_wrapper %}
<div class="post">
    <h1 class="post-title">{{ page.title }}</h1>
    <div>
        {{ page.blocks.content|raw }}
    </div>
</div>
{% endblock %}
